Future Improvements

There are still a few things I want to add to this project once I learn more. One idea is to build a simple interface instead of only using the terminal. I also want to add a monthly summary feature that automatically shows how much I spent in each category.

Another improvement would be to improve data validation so the program can warn the user when they type something wrong. In the future I also want to try exporting reports or graphs using Python libraries. This project can grow slowly as I learn more coding concepts.
